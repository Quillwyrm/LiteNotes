--mdlayout
-- plugins/litenotes/mdlayout.lua

local mdlayout = {}

-- build_layout(blocks, fonts, style, width)
-- future args: width for line wrapping
function mdlayout.build_layout(blocks, fonts, style, width)
  -- STUB:
  -- for now just return nil so NoteReader falls back to draw_plain_text
  return nil
end

return mdlayout

